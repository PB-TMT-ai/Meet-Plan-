# Copy to .env and fill in real values. Never commit .env.

# Anthropic / Claude
ANTHROPIC_API_KEY=

# Google APIs (Sheets, Slides, Drive, Calendar)
# Place OAuth client config in credentials.json; token.json is generated on first auth.
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# Add other service keys below as workflows require them.
